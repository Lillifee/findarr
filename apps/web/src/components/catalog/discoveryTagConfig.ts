import type { DiscoveryType } from '../../utils/catalogSearchParams';
import type { IconName } from '../ui/Icon';

export const discoveryIcons: Record<DiscoveryType, IconName> = {
  genre: 'theater_comedy',
  keyword: 'sell',
  person: 'person',
};
